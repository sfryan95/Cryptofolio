// export function populatePieChart(rowArr, datAarr, colors) {
//   for (let i = 0; i < rowArr.length; i += 1) {
//     datAarr.push({ symbol: rowArr[i].symbol, value: rowArr[i].allocation, color: colors[i] });
//   }
// }

// export function createPieChartData(symbol, allocation, color) {
//   return {
//     symbol: symbol,
//     allocation: allocation,
//     color: color,
//   };
// }

// populateData(Array.from(rows));
// console.log(data);
